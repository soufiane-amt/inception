version: '3'  # Choose the appropriate version here

services:
  mariadb:
    init: true
    build: requirements/mariadb/.
    image: mariadb:inception
    container_name: my-mariadb
    volumes:
      - mariadb_data:/var/lib/mysql
    ports:
      - '3306:3306'
    networks:
      - my-network
    restart: always
    env_file:
      - .env


  nginx:
    build: requirements/nginx/.
    image: nginx:inception
    container_name: my-nginx
    depends_on:
      - wordpress
    ports:
      - '443:443'
    networks:
      - my-network
    volumes:
      - wordpress_data:/var/www/html
    restart: always
    env_file:
      - .env


  wordpress:
    init: true
    build: requirements/wordpress/.
    image: wordpress:inception
    container_name: my-wordpress
    ports:
      - 9000:9000
    networks:
      - my-network
    volumes:
      - wordpress_data:/var/www/html
    restart: always
    env_file:
      - .env


networks:
  my-network:

volumes:
  mariadb_data:
  wordpress_data:
